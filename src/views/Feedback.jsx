import React from 'react';
import PropTypes from 'prop-types';

const Feedback = (props) => {
    return <div>Submit feedback and feature request</div>;
};

Feedback.propTypes = {};

export default Feedback;
