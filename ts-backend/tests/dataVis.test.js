// import { formDayBins } from 'src/utils/dataVisUtils';

// describe('Forming time intervals', () => {
//   test('it should fail when the left bound is over the right bound', () => {
//     const fromTimestamp = Date.now() / 1000 - (24 * 60 * 60);
//     const toTimestamp = Date.now() / 1000 - 2 * (24 * 60 * 60);

//     formDayBins(fromTimestamp, toTimestamp);
//   });
// });
